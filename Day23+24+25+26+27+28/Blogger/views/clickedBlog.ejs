<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('./partials/head.ejs') %>
  <title><%= blog.title %></title>
  <style>
    body {
      background: linear-gradient(135deg, #295e4e 0%, #2e8b57 100%);
      font-family: 'Segoe UI', Arial, sans-serif;
      margin: 0;
      min-height: 100vh;
    }

    .blog-container {
      max-width: 800px;
      margin: 2.5rem auto;
      background: #fff;
      border-radius: 16px;
      box-shadow: 0 4px 24px rgba(0,0,0,0.11);
      padding: 2.5rem 2rem 2rem 2rem;
      position: relative;
    }

    .blog-image {
      width: 100%;
      max-height: 380px;
      object-fit: cover;
      border-radius: 12px;
      margin-bottom: 1.5rem;
      box-shadow: 0 2px 10px rgba(44,62,80,0.07);
    }

    .blog-title {
      font-size: 2.4rem;
      font-weight: 700;
      margin: 0 0 0.5rem 0;
      color: #22482a;
      letter-spacing: 0.01em;
    }

    .blog-meta {
      display: flex;
      align-items: center;
      gap: 0.7rem;
      font-size: 1rem;
      color: #6b7a7f;
      margin-bottom: 1.6rem;
    }
    .author-img {
      width: 44px;
      height: 44px;
      border-radius: 50%;
      object-fit: cover;
      border: 2px solid #2e8b57;
      margin-right: 0.5rem;
    }
    .author-name {
      font-weight: 600;
      color: #22482a;
    }

    .blog-body {
      font-size: 1.17rem;
      color: #2e3d32;
      line-height: 1.7;
      margin-bottom: 2.5rem;
      white-space: pre-line;
    }

    .interaction {
      margin: 2.5rem 0 1.5rem 0;
      display: flex;
      gap: 1.2rem;
      align-items: center;
    }

    .like-btn {
      background: linear-gradient(90deg, #1e8449 70%, #4ac29a 100%);
      color: #fff;
      font-weight: 600;
      padding: 0.6rem 1.4rem;
      border: none;
      border-radius: 30px;
      cursor: pointer;
      font-size: 1.1rem;
      transition: background 0.2s;
      box-shadow: 0 2px 8px rgba(46,139,87,0.08);
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }
    .like-btn:hover {
      background: linear-gradient(90deg, #4ac29a 0%, #1e8449 100%);
    }

    .comment-section {
      margin-top: 2.5rem;
      background: #f6faf7;
      border-radius: 10px;
      padding: 1.5rem 1rem 1rem 1rem;
      box-shadow: 0 1px 4px rgba(44,62,80,0.04);
    }
    .comment-section h3 {
      color: #22482a;
      margin-bottom: 1rem;
      font-size: 1.2rem;
      font-weight: 600;
    }
    .comment-form textarea {
      width: 100%;
      padding: 0.7rem;
      border-radius: 8px;
      border: 1.5px solid #b2d8c5;
      font-size: 1.07rem;
      margin-bottom: 0.7rem;
      resize: vertical;
      min-height: 70px;
      transition: border 0.2s;
    }
    .comment-form textarea:focus {
      border-color: #2e8b57;
      outline: none;
    }
    .comment-form button {
      padding: 0.5rem 1.3rem;
      background: #2e8b57;
      color: #fff;
      border: none;
      border-radius: 7px;
      font-weight: 600;
      cursor: pointer;
      font-size: 1rem;
      transition: background 0.2s;
    }
    .comment-form button:hover {
      background: #1e8449;
    }

    .comments-list {
      margin-top: 1.5rem;
      display: flex;
      flex-direction: column;
      gap: 1.2rem;
    }
    .comment-card {
      background: #fff;
      border-radius: 8px;
      box-shadow: 0 1px 6px rgba(44,62,80,0.06);
      padding: 1rem 1.2rem;
      display: flex;
      gap: 1rem;
      align-items: flex-start;
      border-left: 4px solid #2e8b57;
    }
    .comment-avatar {
      width: 38px;
      height: 38px;
      border-radius: 50%;
      object-fit: cover;
      margin-right: 0.5rem;
      border: 1.5px solid #b2d8c5;
    }
    .comment-content {
      flex: 1;
    }
    .comment-author {
      font-weight: 600;
      color: #22482a;
      margin-bottom: 0.2rem;
    }
    .comment-date {
      font-size: 0.89rem;
      color: #7a8a81;
      margin-bottom: 0.3rem;
    }
    .comment-text {
      color: #2e3d32;
      font-size: 1.06rem;
      line-height: 1.5;
    }

    @media (max-width: 600px) {
      .blog-container {
        padding: 1rem 0.5rem;
      }
      .blog-title {
        font-size: 1.5rem;
      }
      .blog-meta {
        flex-direction: column;
        align-items: flex-start;
        gap: 0.2rem;
      }
      .comment-card {
        flex-direction: column;
        align-items: flex-start;
        gap: 0.6rem;
      }
    }
  </style>
</head>
<body>
  <%- include('./partials/nav.ejs') %>

  <main>
    <article class="blog-container">
      <img src="<%= blog.coverImageUrl %>" alt="Cover Image" class="blog-image">
      <header>
        <h1 class="blog-title"><%= blog.title %></h1>
        <div class="blog-meta">
          <img src="<%= user.createdBy.profileImageUrl %>" alt="Author" class="author-img">
          <span class="author-name"><%= user.createdBy.full_Name %></span>
          <span>on <%= new Date(blog.createdAt).toUTCString() %></span>
        </div>
      </header>
      <section class="blog-body"><%= blog.body %></section>

      <% if (locals.loggedInUser) { %>
        <div class="interaction">
          <button class="like-btn" onClick="isliked()">
            ❤️ Like (<%= blog.likes || 0 %>)
          </button>
        </div>
        <section class="comment-section">
          <h3>Leave a Comment:</h3>
          <form class="comment-form" action="/blog/<%= blog._id%>" method="POST">
            <textarea name="comment" rows="4" placeholder="Write your comment here..." required></textarea>
            <br>
            <button type="submit">Submit</button>
          </form>
          <% if (locals.comments && comments.length > 0) { %>
            <div class="comments-list">
              <% comments.forEach(comment => { %>
                <div class="comment-card">
                  <img src="<%= comment.createdBy.profileImageUrl %>" alt="User" class="comment-avatar">
                  <div class="comment-content">
                    <div class="comment-author"><%= comment.createdBy.full_Name %></div>
                    <div class="comment-date"><%= new Date(comment.createdAt).toUTCString() %></div>
                    <div class="comment-text"><%= comment.comment %></div>
                  </div>
                </div>
              <% }) %>
            </div>
          <% } %>
        </section>
      <% } else { %>
        <hr>
        <div style="margin-top:1.5rem;">
          <a class="like-btn" href="/user/signin" style="text-decoration:none;display:inline-block;">Login to interact &amp; comment</a>
        </div>
      <% } %>
    </article>
  </main>
  <%- include('./partials/scripts.ejs') %>
</body>
</html>
